{$SITE_ADDRESS} {
	encode gzip

	# Menu images are immutable once uploaded — cache hard to cut egress.
	@uploads path /uploads/*
	header @uploads Cache-Control "public, max-age=31536000, immutable"

	reverse_proxy app:3001
}

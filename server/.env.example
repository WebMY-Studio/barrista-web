# Admin login (set these on your server)
ADMIN_USER=admin
ADMIN_PASSWORD=your-secure-password

# Secret for JWT signing (use a long random string in production)
JWT_SECRET=your-jwt-secret

# Optional: token expiry (default 7d)
# JWT_EXPIRES_IN=7d
